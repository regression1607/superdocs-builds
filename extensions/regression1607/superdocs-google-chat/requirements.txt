fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pytest>=8.0
# Optional: only needed in production to verify Google Chat's signed JWT
# (app.auth imports it lazily; tests and local dev run without it).
google-auth>=2.28
